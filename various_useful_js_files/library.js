function cum() {
	return "~~~~~~~~~~~"
}

export function literallyCummingEverywhere() {
	const dick = "8===D"
	
	let cum_everywhere = dick
	
	if (face instanceof Face) {
		cum_everywhere += cum() + face
	} else {
		cum_everywhere += cum()
		
		for(let i = 0; i >= Math.floor(Math.random() * 5); i++) {
			cum_everywhere += "\n"
			cum_everywhere += cum()
		}
	}
}